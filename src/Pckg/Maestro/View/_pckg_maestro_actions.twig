<script type="text/javascript">
    var pckgMaestroActionsComponent = Vue.component('pckg-maestro-actions', {
        mixins: [pckgDelimiters],
        name: 'pckg-maestro-actions',
        template: '#pckg-maestro-actions',
        props: {
            record: {
                type: Object,
                required: true
            },
            recordactionhandler: {
                /*type: Function,
                 required: true*/
            }
        },
        methods: {
            recordAction: function (record, action) {
                console.log('Yeeey, caught maestro action record:' + action + ', dispatching');

                $dispatcher.$emit('record:' + action, record, record.id);
            }
        }
    });
</script>