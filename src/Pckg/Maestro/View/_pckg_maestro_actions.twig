<script type="text/javascript">
    var pckgMaestroActionsComponent = Vue.component('pckg-maestro-actions', {
        name: 'pckg-maestro-actions',
        template: '#pckg-maestro-actions',
        props: {
            record: {
                type: Object,
                required: true
            },
            recordactionhandler: {
                /*type: Function,
                required: true*/
            }
        },
        methods: {
            recordAction: function (record, action) {
                console.log('Yeeey, caught record:' + action + ', dispatching, emiting and broadcasting');

                //this.$root.$dispatch('record:' + action, record.id, record);
                //this.$root.$emit('record:' + action, record.id, record);
                this.$root.$broadcast('record:' + action, record.id, record);
            }
        }
    });
</script>