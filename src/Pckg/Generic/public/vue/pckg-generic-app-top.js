/**
 * Register main Vue event dispatcher
 * @type {Vue}
 */
$dispatcher = new Vue();

var data = data || {};

var $vue = $vue || {};

var props = props || {};

var pckgDelimiters = {
    delimiters: ['${', '}']
};