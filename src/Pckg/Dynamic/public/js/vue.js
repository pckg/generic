(function (Vue, $, data, http) {

    /*var formActionMorph = new Vue({
        el: '#formActionMorph',
        data: function () {
            return {
                form: {
                    action_id: null,
                    morph_id: null,
                    poly_id: null,
                    content_id: null,
                    variable_id: null,
                    order: 0
                }
            };
        },
        methods: {
            onSubmit: function (e) {
                e.preventDefault();

                http.submitForm(this, ['action_id', 'morph_id', 'poly_id', 'content_id', 'variable_id', 'order']);

                return false;
            }
        }
    });*/

})(Vue, jQuery, data, http);